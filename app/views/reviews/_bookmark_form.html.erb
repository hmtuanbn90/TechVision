<%= form_for [@review, @bookmark], remote: true do |f| %>
  <%= f.hidden_field :review_id, value: @review.id %>
  <%= f.submit t(:bookmark, scope: "reviews.show"), id: "bookmark_button", class: "btn btn-primary " %>
<% end %>
